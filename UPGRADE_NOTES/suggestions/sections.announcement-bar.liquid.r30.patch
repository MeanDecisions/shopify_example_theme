diff --git a/sections/announcement-bar.liquid b/sections/announcement-bar.liquid
index fa43f4d..ac425f2 100644
--- a/sections/announcement-bar.liquid
+++ b/sections/announcement-bar.liquid
@@ -12,10 +12,14 @@

 {%- if show_section -%}
   <style>
-    :root {--topbar-height: var(--sp-12);}
+    :root {
+      --topbar-height: var(--sp-12);
+    }

     @media screen and (min-width: 1536px) {
-      :root {--topbar-height: var(--sp-14);}
+      :root {
+        --topbar-height: var(--sp-14);
+      }
     }

     #shopify-section-{{ section.id }} {
@@ -27,7 +31,7 @@
   </style>

   {%- liquid
-    assign announcement_block_count = section.blocks.size
+    assign announcements_count = section.blocks.size
     if section.settings.enable_language_selector and localization.available_languages.size > 1
       assign enable_language_selector = true
     endif
@@ -36,94 +40,31 @@
     endif
   -%}

-  <div class="topbar topbar--{{ section.settings.announcement_position }}{% if section.settings.invert_icons_selectors %} topbar--inverted{% endif %}{% unless section.settings.show_social or enable_language_selector or enable_country_selector %} topbar--only{% endunless %} page-width{% if section.settings.full_width %} page-width--full{% endif %} section{% if section.settings.rounded %} section--rounded{% endif %} relative grid py-0 h-full w-full items-center">
+  <nav class="topbar topbar--{{ section.settings.announcement_position }}{% if section.settings.invert_icons_selectors %} topbar--inverted{% endif %}{% unless section.settings.show_social or enable_language_selector or enable_country_selector %} topbar--only{% endunless %} page-width{% if section.settings.full_width %} page-width--full{% endif %} section{% if section.settings.rounded %} section--rounded{% endif %} relative grid py-0 h-full w-full items-center" aria-label="{{ 'sections.announcements.announcement_bar' | t | escape }}">
     {%- if section.settings.show_social -%}
       <div class="social-icons hidden lg:flex items-center">
         {%- render 'social-icons', size: 'sm', gap: '6' -%}
       </div>
     {%- endif -%}

-    {%- if announcement_block_count > 0 -%}
-      <div class="announcement-bar h-full flex justify-center lg:justify-{{ section.settings.announcement_position }}{% unless section.settings.show_navigation %} without-nav{% endunless %} relative">
-        <announcement-bar id="Slider-{{ section.id }}" class="announcement__slides{% if announcement_block_count > 1 %} flex justify-center w-full h-full flickity{% endif %}"{% if section.settings.autoplay %} autoplay autoplay-speed="{{ section.settings.autoplay_speed }}"{% endif %}>
-          {%- liquid
-            assign with_gradient = false
-            if section.settings.gradient_highlight != blank and section.settings.gradient_highlight != 'rgba(0,0,0,0)'
-              assign with_gradient = true
-            endif
-          -%}
-          {%- for block in section.blocks -%}
-            <div class="announcement__slide flex h-full"{% unless forloop.first %} aria-hidden="true"{% endunless %} {{ block.shopify_attributes }}>
-              {%- if block.settings.link != blank -%}
-                <a class="announcement__content flex items-center gap-2" href="{{ block.settings.link }}">
-              {%- else -%}
-                <div class="announcement__content flex items-center gap-2">
-              {%- endif -%}
-                {%- if block.settings.icon != 'none' -%}
-                  <span class="announcement-icon{% if block.settings.hide_icon_mobile %} hidden md:block{% endif %}">
-                    {%- render 'icon-announcement', icon: block.settings.icon, size: 'xs', class: 'stroke-1' -%}
-                  </span>
-                {%- endif -%}
-
-                {%- if block.settings.text_mobile != blank -%}
-                  <p class="announcement-text leading-tight md:hidden">
-                    {%- render 'highlighted-text', content: block.settings.text_mobile, style: block.settings.highlighted_text, scribble: block.settings.highlighted_scribble, gradient: with_gradient -%}
-                  </p>
-                  <p class="announcement-text leading-tight hidden md:block">
-                    {%- render 'highlighted-text', content: block.settings.text, style: block.settings.highlighted_text, scribble: block.settings.highlighted_scribble, gradient: with_gradient -%}
-                  </p>
-                {%- else -%}
-                  <p class="announcement-text leading-tight">
-                    {%- render 'highlighted-text', content: block.settings.text, style: block.settings.highlighted_text, scribble: block.settings.highlighted_scribble, gradient: with_gradient -%}
-                  </p>
-                {%- endif -%}
-
-              {%- if block.settings.link != blank -%}
-                </a>
-              {%- else -%}
-                </div>
-              {%- endif -%}
-            </div>
-          {%- endfor -%}
-        </announcement-bar>
-        {%- if section.settings.show_navigation and announcement_block_count > 1 -%}
-          <button type="button" is="previous-button" class="fader left relative flex items-center justify-start h-full" aria-controls="Slider-{{ section.id }}" aria-label="{{ 'general.pagination.previous' | t | escape }}">
-            <svg class="icon icon-arrow-left icon-xs transform" viewBox="0 0 15 14" fill="none" stroke="currentColor" xmlns="http://www.w3.org/2000/svg">
-              <path stroke-linecap="round" stroke-linejoin="round" d="M6.33333 2.91675L2.25 7.00004M2.25 7.00004L6.33333 11.0834M2.25 7.00004H12.75"></path>
-            </svg>
-          </button>
-          <button type="button" is="next-button" class="fader right relative flex items-center justify-end h-full" aria-controls="Slider-{{ section.id }}" aria-label="{{ 'general.pagination.next' | t | escape }}">
-            <svg class="icon icon-arrow-right icon-xs transform" viewBox="0 0 15 14" fill="none" stroke="currentColor" xmlns="http://www.w3.org/2000/svg">
-              <path stroke-linecap="round" stroke-linejoin="round" d="M2.25 7.00004H12.75M12.75 7.00004L8.66667 2.91675M12.75 7.00004L8.66667 11.0834"></path>
-            </svg>
-          </button>
-          {%- if section.settings.autoplay -%}
-            <button
-              type="button"
-              class="slideshow-control flex items-center justify-center rounded-full sr-only"
-              is="control-button"
-              aria-controls="Slider-{{ section.id }}"
-              aria-live="polite"
-            >
-              <span class="slideshow-control__pause">
-                {%- render 'icon', icon: 'pause' -%}
-                <span class="sr-only">{{ 'sections.slideshow.pause_slideshow' | t }}</span>
-              </span>
-              <span class="slideshow-control__play">
-                {%- render 'icon', icon: 'play' -%}
-                <span class="sr-only">{{ 'sections.slideshow.play_slideshow' | t }}</span>
-              </span>
-            </button>
-          {%- endif -%}
-        {%- endif -%}
-      </div>
-    {%- endif -%}
+    {%- liquid
+      if announcements_count > 0
+        if section.settings.announcement_layout == 'carousel'
+          render 'announcements-carousel', section: section, announcements_count: announcements_count
+        else
+          render 'announcements-marquee', section: section, announcements_count: announcements_count
+        endif
+      endif
+    -%}

     {%- if enable_language_selector or enable_country_selector -%}
       <div class="localization hidden lg:flex items-center h-full">
         {%- if enable_language_selector -%}
           <div class="localization__item h-full relative" role="combobox" aria-controls="LanguageLocalization-{{ section.id }}" aria-expanded="false" aria-label="{{ 'localization.language_label' | t | escape }}">
-            <button type="button" class="flex items-center gap-3 h-full z-2 relative"{% unless section_rendering_api %} is="api-button" data-id="announcement_localization_language_form" data-section-id="{{ section.id }}"{% endunless %}>
+            <button type="button" class="flex items-center gap-3 h-full z-2 relative">
+              {%- unless section_rendering_api -%}
+                <api-button class="api-only" data-id="announcement_localization_language_form" data-section-id="{{ section.id }}"></api-button>
+              {%- endunless -%}
               {%- render 'icon', icon: 'language', size: 'xs', class: 'stroke-1 hidden xl:block' -%}
               <span class="leading-tight">{{ localization.language.endonym_name | capitalize }}</span>
               {%- render 'icon', icon: 'chevron-down', size: 'xs' -%}
@@ -163,7 +104,10 @@
         {%- endif -%}
         {%- if enable_country_selector -%}
           <div class="localization__item h-full relative" role="combobox" aria-controls="CountryLocalization-{{ section.id }}" aria-expanded="false" aria-label="{{ 'localization.country_label' | t | escape }}">
-            <button type="button" class="flex items-center gap-3 h-full z-2 relative"{% unless section_rendering_api %} is="api-button" data-id="announcement_localization_country_form" data-section-id="{{ section.id }}"{% endunless %}>
+            <button type="button" class="flex items-center gap-3 h-full z-2 relative">
+              {%- unless section_rendering_api -%}
+                <api-button class="api-only" data-id="announcement_localization_country_form" data-section-id="{{ section.id }}"></api-button>
+              {%- endunless -%}
               {%- render 'icon', icon: 'currency', size: 'xs', class: 'stroke-1 hidden xl:block' -%}
               <span class="leading-tight">{{ localization.country.name }} ({{ localization.country.currency.iso_code }} {{ localization.country.currency.symbol }})</span>
               {%- render 'icon', icon: 'chevron-down', size: 'xs' -%}
@@ -201,7 +145,7 @@
         {%- endif -%}
       </div>
     {%- endif -%}
-  </div>
+  </nav>
 {%- endif -%}

 {% schema %}
@@ -262,12 +206,30 @@
       "info": "t:sections.announcement-bar.settings.announcement_position.info"
     },
     {
+      "type": "select",
+      "id": "announcement_layout",
+      "options": [
+        {
+          "value": "carousel",
+          "label": "t:sections.announcement-bar.settings.announcement_layout.options__1.label"
+        },
+        {
+          "value": "marquee",
+          "label": "t:sections.announcement-bar.settings.announcement_layout.options__2.label"
+        }
+      ],
+      "default": "carousel",
+      "label": "t:sections.announcement-bar.settings.announcement_layout.label"
+    },
+    {
+      "visible_if": "{{ section.settings.announcement_layout == 'carousel' }}",
       "type": "checkbox",
       "id": "autoplay",
       "default": true,
       "label": "t:sections.announcement-bar.settings.autoplay.label"
     },
     {
+      "visible_if": "{{ section.settings.announcement_layout == 'carousel' and section.settings.autoplay == true }}",
       "type": "range",
       "id": "autoplay_speed",
       "label": "t:sections.announcement-bar.settings.autoplay_speed.label",
@@ -278,11 +240,40 @@
       "default": 5
     },
     {
+      "visible_if": "{{ section.settings.announcement_layout == 'carousel' }}",
       "type": "checkbox",
       "id": "show_navigation",
       "default": false,
       "label": "t:sections.announcement-bar.settings.show_navigation.label"
     },
+    {
+      "visible_if": "{{ section.settings.announcement_layout == 'marquee' }}",
+      "type": "select",
+      "id": "direction",
+      "options": [
+        {
+          "value": "left",
+          "label": "t:sections.scrolling-text.settings.direction.options__1.label"
+        },
+        {
+          "value": "right",
+          "label": "t:sections.scrolling-text.settings.direction.options__2.label"
+        }
+      ],
+      "default": "left",
+      "label": "t:sections.scrolling-text.settings.direction.label"
+    },
+    {
+      "visible_if": "{{ section.settings.announcement_layout == 'marquee' }}",
+      "type": "range",
+      "id": "speed",
+      "min": 1,
+      "max": 30,
+      "step": 1,
+      "label": "t:sections.scrolling-text.settings.speed.label",
+      "unit": "x",
+      "default": 20
+    },
     {
       "type": "header",
       "content": "t:sections.global.settings.header__colors.content",
@@ -343,7 +334,7 @@
     {
       "type": "announcement",
       "name": "t:sections.announcement-bar.blocks.announcement.name",
-      "limit": 3,
+      "limit": 6,
       "settings": [
         {
           "type": "select",
@@ -649,6 +640,7 @@
           "label": "t:sections.global.settings.highlighted_text.label"
         },
         {
+          "visible_if": "{{ block.settings.highlighted_text == 'scribble' }}",
           "type": "select",
           "id": "highlighted_scribble",
           "options": [
